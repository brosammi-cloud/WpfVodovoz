<Window x:Class="WpfVodovoz.Views.OrderCreateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfVodovoz.Views"
        mc:Ignorable="d"
        Title="OrderCreateView" Height="450" Width="800">
    <Grid Margin="10">

        <StackPanel>

            <Label Content="Номер заказа" />
            <TextBox Text="{Binding NewOrder.OrderNum}" Margin="0,0,0,10"/>

            <Label Content="Дата заказа" />
            <DatePicker SelectedDate="{Binding NewOrder.OrderDate}" Margin="0,0,0,10"/>
            
            <Label Content="Сумма заказа" />
            <TextBox Text="{Binding NewOrder.Amount}" Margin="0,0,0,10"/>

            <Label Content="Сотрудник" />
            <ComboBox ItemsSource="{Binding Employees}"
                      SelectedValue="{Binding SelectedEmployeeId}"
                      SelectedValuePath="EmployeeId"
                      DisplayMemberPath="FullName"/>

            <Label Content="Контрагент" />
            <ComboBox ItemsSource="{Binding Contractors}"
                      SelectedValue="{Binding SelectedContractorId}"
                      SelectedValuePath="СontractorId"
                      DisplayMemberPath="Name"/>

            <Button Content="Сохранить"
                Height="30"
                Width="120"
                HorizontalAlignment="Right"
                Command="{Binding SaveCommand}"/>

        </StackPanel>

    </Grid>
</Window>
