<Window x:Class="WpfVodovoz.Views.EmployeeCreateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfVodovoz.Views"
        mc:Ignorable="d"
        Title="Создать сотрудника"
        Height="300" Width="400">

    <Grid Margin="10">

        <StackPanel>

            <Label Content="ФИО" />
            <TextBox Text="{Binding NewEmployee.FullName}" Margin="0,0,0,10"/>
            <Label Content="Должность" />
            <ComboBox SelectedItem="{Binding NewEmployee.Position}"
                      Margin="0,0,0,10"
                      ItemsSource="{Binding Positions}"/>
            <Label Content="Дата рождения" />
            <DatePicker SelectedDate="{Binding NewEmployee.BirthDate}" Margin="0,0,0,10"/>

            <Button Content="Сохранить"
                    Height="30"
                    Width="120"
                    HorizontalAlignment="Right"
                    Command="{Binding SaveCommand}"/>

        </StackPanel>

    </Grid>
</Window>
