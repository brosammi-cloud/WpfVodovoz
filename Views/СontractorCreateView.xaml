<Window x:Class="WpfVodovoz.Views.ContractorCreateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfVodovoz.Views"
        mc:Ignorable="d"
        Title="Создать контрагента" Height="450" Width="800">
    <Grid Margin="10">

        <StackPanel>

            <Label Content="Название контрагента" />
            <TextBox Text="{Binding NewСontractor.Name}" Margin="0,0,0,10"/>
            <Label Content="ИНН" />
            <TextBox Text="{Binding NewСontractor.INN}" Margin="0,0,0,10"/>
            <Label Content="Ответственный" />
            <ComboBox ItemsSource="{Binding Employees}"
                      SelectedValue="{Binding SelectedEmployeeId}"
                      SelectedValuePath="EmployeeId"
                      DisplayMemberPath="FullName"/>

            <Button Content="Сохранить"
                Height="30"
                Width="120"
                HorizontalAlignment="Right"
                Command="{Binding SaveCommand}"/>

        </StackPanel>

    </Grid>
</Window>
